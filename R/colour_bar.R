# colour_bar <- function(palette, n = 100){
#   pal <- colorRampPalette(palette)(n)
#
# }
#
# col2values <- seq(min(result$values), max(result$values), length.out =
#                     ncols)
#
#
# plot.hemi <- result %>% filter(grepl("lh_",label))
# nmin <- which.min(abs(col2values - min(plot.hemi$values)))
# nmax <- which.min(abs(col2values - max(plot.hemi$values)))
